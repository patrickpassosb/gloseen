# Task: ESP32-CAM Hardware Setup

## Objective
Configure and flash the ESP32-CAM with the `blindur.ino` firmware to enable the Vision AI Pro features.

## Success Criteria
- [ ] ESP32-CAM flashed successfully.
- [ ] ESP32-CAM connected to Wi-Fi.
- [ ] Camera IP address identified.
- [ ] Python backend (`app.py`) updated with the camera IP.
- [ ] Image capture verified from the Python backend.

## TO-DO
- [x] Guide user on Arduino IDE setup (ESP32 Board Manager).
- [x] Guide user on Wi-Fi configuration in `blindur.ino`.
- [x] Fix compilation error (Downgraded to ESP32 core v2.0.17).
- [x] Discover buttons on the hardware.
- [/] Troubleshoot hardware connection (Device not appearing in dmesg).
- [ ] Flash the device using the buttons (RST + BOOT).
- [ ] Verify connection and get IP.
- [ ] Update `.env` or `app.py`.

## Progress
- Initial task defined.
